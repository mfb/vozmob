In order to execute the vozmob callback script for a given number, just run

   vozmob_callback_dial($number);

That function will dial the number, ask the user to record a message, and then create a node of type 'story' with a link to the message and some meta-information.


